// Expressions - производят значения
2 + 2
37
true
true && false

//Statement - больший кусок кода, который запускается и не производит какого-то значения сам по себе

const number = 10;

// if statement
if (number > 0) {
  const text = 'The number is positive';
}

console.log(`{number - 2} is also positive number`); 